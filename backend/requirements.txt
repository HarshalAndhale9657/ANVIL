# ── Web framework / server ───────────────────────────────────────────────────
fastapi>=0.104.0,<1.0.0
uvicorn>=0.24.0,<1.0.0
pydantic>=2.5.0,<3.0.0
sse-starlette>=1.8.0,<3.0.0
python-multipart>=0.0.6
python-dotenv>=1.0.0,<2.0.0
itsdangerous>=2.1.0,<3.0.0

# ── Observability (Omium + OpenTelemetry) ────────────────────────────────────
opentelemetry-api>=1.20.0,<2.0.0
opentelemetry-sdk>=1.20.0,<2.0.0
opentelemetry-exporter-otlp>=1.20.0,<2.0.0
omium>=0.4.0

# ── Integrations ─────────────────────────────────────────────────────────────
openai>=1.6.0,<2.0.0
PyGithub>=2.1.0,<3.0.0
httpx>=0.25.0,<1.0.0

# ── Runtime for sandboxed exploit payloads + launching Flask target apps ─────
requests>=2.31.0,<3.0.0
Flask>=3.0.0,<4.0.0

# ── Tests ────────────────────────────────────────────────────────────────────
pytest>=7.4.0,<9.0.0
